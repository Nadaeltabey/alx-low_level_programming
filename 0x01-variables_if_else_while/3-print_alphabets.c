#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char (alphabet);
	char (Alphabet);

	if (alphabet = 'a')alphabet <= 'z'; alphabet++;
	{
		putchar(alphabet);
	}
	if (Alphabet = 'A')Alphabet <= 'Z'; Alphabet++;
	{
	 	putchar(Alphabet);
	}
	putchar('\n');
    return (0);
}
