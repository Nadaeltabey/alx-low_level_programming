#include"main.h"

/**
 * _abs  - function to give the positive
 * of any number
 *@r : operand
 *Return:  1 (Success) and 0 (failure)
*/
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}
}
