#include"main.h"

/**
 * mul  - function to give the muliplication
 * of two numbers
 * @a: operand
 * @b: operand
 *
 *Return:  1 (Success) and 0 (failure)
*/
int mul(int a, int b)
{
	/*int y = a * b;*/
	return (a * b);
}
