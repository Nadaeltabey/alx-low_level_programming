#include"main.h"

/**
 * _puts - function to give
 * a string
 * @str: operand
 *Return:  1 (Success) and 0 (failure)
*/
char *_strcpy(char *dest, char *src)
{
int c = 0, i;

while (*src != '\0')
{ 
c++;
src++;
}
for (i = 0; src[i] <= '\0'; i++)
{
src[i] == dest[i];
}
}
