#include"main.h"

/**
 * _abs  - function to give the positive 
 * of any number
 *
 *Return:  1 (Success) and 0 (failure)
*/
int _abs(int n)
int m;
{
if (n >= 0)
{
_putchar(n + 0);
}
else
{
m = n * -1;
_putchar(m + 0);
}
