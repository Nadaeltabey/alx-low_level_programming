#include"main.h"

/**
 * add - function to give the
 * sum of two  numbers
 *@a : first operand
 *@b : second operand
 *Return:  1 (Success) and 0 (failure)
*/
int add(int a, int b)
{
return (a + b);
}
